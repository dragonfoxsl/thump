# thump Handoff

- This project publishes a GHCR container. The PyPI project named `thump` is
  unrelated and must not be modified.
- Scheduled and manual CI verify builds only. Publishing and signing stay push-only.
